# Workflow How-Tos

`docs/workflows/` contains workflow-specific procedures.  These docs are not
required reading for every coding agent; read the relevant one when working on
that pipeline.

- [`act_train_eval_howto.md`](act_train_eval_howto.md) — ACT qpos-target
  train/eval conventions.
- [`eval_scene_init_howto.md`](eval_scene_init_howto.md) — deterministic
  scene-init eval set generation and usage.
